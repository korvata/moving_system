spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/marketdesigners?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: qwer1234!


  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        show_sql : true

  logging:
    level:
      org.hibernate.SQL: debug